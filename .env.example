# Copy this file to .env.local and fill in your values.
# For deployment, set these same variables in your host's dashboard (Vercel, etc.).
# Do NOT commit .env.local or real values to Git.

# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Same values for server-side (magic link + auth exchange). Required on Vercel for "No API key" fix.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Supabase service role (required for admin: invite users, init checklist)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Your app URL – must include https:// in production (e.g. https://your-app.vercel.app). Missing protocol breaks magic link.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: n8n webhook for email (invite, magic link)
# N8N_WEBHOOK_URL=https://your-n8n-instance/webhook/...
